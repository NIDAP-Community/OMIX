% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat-bridge.R
\name{omix_seurat_to_expression_input}
\alias{omix_seurat_to_expression_input}
\alias{omix_read_seurat_expression_rds}
\title{Create a donor-level mean-expression input from one Seurat cell type}
\usage{
omix_seurat_to_expression_input(
  seurat_object,
  donor_column,
  group_column,
  cell_type_column,
  cell_type,
  sample_metadata_columns = NULL,
  cell_filter_column = NULL,
  cell_filter_values = NULL,
  assay = "RNA",
  layer = "data",
  feature_id_column = "GeneName",
  min_cells = 20L,
  on_insufficient_cells = c("error", "drop")
)

omix_read_seurat_expression_rds(path, ...)
}
\arguments{
\item{seurat_object}{A Seurat object supported by SeuratObject.}

\item{donor_column}{Cell-metadata column identifying biological donors.}

\item{group_column}{Cell-metadata column identifying the experimental group.}

\item{cell_type_column}{Cell-metadata column identifying cell types.}

\item{cell_type}{One exact cell-type value to aggregate.}

\item{sample_metadata_columns}{Optional cell-metadata columns to retain in
the pseudobulk sample metadata. Each value must be non-missing and
invariant within every donor-by-group profile.}

\item{cell_filter_column}{Optional cell-metadata column used for an explicit
pre-aggregation filter.}

\item{cell_filter_values}{Values to retain from `cell_filter_column`. Supply
both filter arguments or neither.}

\item{assay}{Assay containing a continuous gene-expression layer.}

\item{layer}{Layer containing a declared corrected expression matrix. The
default `"data"` is only a placeholder; callers must explicitly verify the
source layer's provenance before treating it as Harmony-corrected data.}

\item{feature_id_column}{Output feature-ID column name. Default:
`"GeneName"`.}

\item{min_cells}{Minimum selected cells per donor-by-group profile. Default:
`20`.}

\item{on_insufficient_cells}{Whether under-populated profiles cause an error
(default) or are dropped.}

\item{path}{Path to an `.rds` file containing a Seurat object.}

\item{...}{Arguments passed to `omix_seurat_to_expression_input()`.}
}
\value{
An `omix_expression_input` with `expression`, aligned `metadata`,
  sample columns, and provenance.

An `omix_expression_input` object.
}
\description{
Selects one cell type from an explicitly declared continuous gene-expression
layer and calculates one arithmetic mean per donor and group. It does not
normalize, batch-correct, transform, or otherwise modify the supplied layer.
This function is appropriate only when the input layer is already the
validated expression representation required by the downstream model.
}
