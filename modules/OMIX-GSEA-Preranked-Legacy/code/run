#!/usr/bin/env bash
set -euo pipefail

# OMIX GSEA - Reproducible Run entrypoint
# Passes all parameters to main.R

cd /code

# Copy documentation guide to results if it exists
if [ -f "README.md" ]; then
  cp README.md /results
fi

Rscript main.R "$@"
